<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to  "Smooth", root_path, class:"navbar-brand" %>
  <div class="collapse navbar-collapse" >
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <% if user_signed_in? && current_user%>
          <%= image_tag(current_user.avatar, width:"30px", height:"30px", class:"rounded-circle ml-5") if current_user.avatar.attached? %>
          <%= @welcome %>
        <% end %>
      </li>
      <li class="nav-itm" >
        <% if user_signed_in?%>
          <%= link_to  "logout", destroy_user_session_path, method: :delete, class:"nav-link" %>
          <div class="settings">
            <%= fa_icon "cog", class: "text-muted " %><%= link_to  "settings", edit_user_registration_path, class:"nav-link"%>
          </div>
        <% else %>
          <%= link_to "login", new_user_session_path, class:"login" %>
          <%= link_to "Sign Up", new_user_registration_path,class:"signup" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
